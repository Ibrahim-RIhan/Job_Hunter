import React from 'react';

const JobDetails = (props) => {
    return (
        <div>
    console.log(props.jobData);
    <h1>HEllo World </h1>
   console.log(jobData);
        </div>
    );
};

export default JobDetails;